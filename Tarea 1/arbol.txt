 1
2 3
2 3
  3

Recursivo
se aplica la misma función para cada hijo
se compara un "res" con el valor de ese valor en ese momento
si es más grande, cambio valor de res

res=0
(Defun altura(lst))
if lst está vacia
  return 1
for lst tiene siguiente
  tmp = altura(car lst)
  if tmp > res
    res=res++
